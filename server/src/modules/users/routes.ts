import { Router } from 'express';

export const usersRouter = Router();

// Placeholder endpoints para validar la estructura
usersRouter.get('/', (_req, res) => {
  res.json({ message: 'Listado de usuarios (placeholder)' });
});


